var path = require("path");
var expect = require('chai').expect;
var secretPrint = require("../src/secretprint");

describe('secretPrint', function() {
    it('should not error out when printing a file to a specific printer');
});